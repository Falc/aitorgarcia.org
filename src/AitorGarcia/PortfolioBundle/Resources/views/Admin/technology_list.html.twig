{% extends "::layout.html.twig" %}

{% block stylesheets %}
{% stylesheets filter="cssrewrite,?yui_css" output='css/backend-*.css'
    'css/main.css'
    'css/tables.css'
%}
    <link href="{{ asset_url }}" rel="stylesheet" type="text/css" />
{% endstylesheets %}
{% endblock %}

{% block subnavigation %}
<ul class="actions">
    <li class="active"><a href="{{ path('admin_technology_list') }}">{{ 'subnavigation.admin.list' | trans }}</a></li>
    <li><a href="{{ path('admin_technology_create') }}">{{ 'subnavigation.admin.create' | trans }}</a></li>
</ul>
{{ parent() }}
<div class="clear"></div>
{% endblock %}

{% block page %}
{% for type, messages in app.session.flashbag.all() %}
    {% for message in messages %}
        <div class="message-{{ type }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}

<table>
    <thead>
        <tr>
            <th class="shrink">{{ 'technologies.admin.id' | trans }}</th>
            <th>{{ 'technologies.admin.name' | trans }}</th>
            <th class="shrink">{{ 'technologies.admin.actions' | trans }}</th>
        </tr>
    </thead>
    <tbody>
    {% for technology in technologies %}
        <tr>
            <td class="centered">{{ technology.id }}</td>
            <td>{{ technology.name }}</td>
            <td class="centered">
                <a class="button-img" href="{{ path('admin_technology_edit', { 'id': technology.id }) }}" title="{{ 'technologies.admin.action_edit' | trans }}"><img src="/bundles/aitorgarciaportfolio/images/edit.png" alt="{{ 'technologies.admin.action_edit' | trans }}" /></a>
                <a class="button-img" href="{{ path('admin_technology_delete', { 'id': technology.id }) }}" title="{{ 'technologies.admin.action_delete' | trans }}"><img src="/bundles/aitorgarciaportfolio/images/delete.png" alt="{{ 'technologies.admin.action_delete' | trans }}" /></a>
            </td>
        </tr>
    {% else %}
        <tr>
            <td class="centered" colspan="3">{{ 'technologies.empty_list' | trans }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
