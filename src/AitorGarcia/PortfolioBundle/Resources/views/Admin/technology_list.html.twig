{% extends "::layout.html.twig" %}

{% block title %}{{ 'technologies.title.list' | trans }} - Aitor García (Backend){% endblock %}

{% block stylesheets %}
{% stylesheets filter="cssrewrite,?yui_css" output='css/backend-*.css'
    'css/main.css'
    'css/tables.css'
%}
    <link href="{{ asset_url }}" rel="stylesheet" type="text/css" />
{% endstylesheets %}
{% endblock %}

{% block subnavigation %}
<ul class="actions">
    <li class="active"><a href="{{ path('admin_technology_list') }}">{{ 'button.list' | trans }}</a></li>
    <li><a href="{{ path('admin_technology_create') }}">{{ 'button.create' | trans }}</a></li>
</ul>
{{ parent() }}
<div class="clear"></div>
{% endblock %}

{% block page %}
{% for type, messages in app.session.flashbag.all() %}
    {% for message in messages %}
        <div class="message-{{ type }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}

<table>
    <thead>
        <tr>
            <th class="shrink">{{ 'field.id' | trans }}</th>
            <th>{{ 'field.name' | trans }}</th>
            <th class="shrink">{{ 'field.actions' | trans }}</th>
        </tr>
    </thead>
    <tbody>
    {% for technology in technologies %}
        <tr>
            <td class="centered">{{ technology.id }}</td>
            <td>{{ technology.name }}</td>
            <td class="centered">
                <a class="button-img" href="{{ path('admin_technology_edit', { 'id': technology.id }) }}" title="{{ 'button.edit_ext' | trans }}"><img src="/bundles/aitorgarciaportfolio/images/edit.png" alt="{{ 'button.edit' | trans }}" /></a>
                <a class="button-img" href="{{ path('admin_technology_delete', { 'id': technology.id }) }}" title="{{ 'button.delete_ext' | trans }}"><img src="/bundles/aitorgarciaportfolio/images/delete.png" alt="{{ 'button.delete' | trans }}" /></a>
            </td>
        </tr>
    {% else %}
        <tr>
            <td class="centered" colspan="3">{{ 'technologies.message.empty_list' | trans }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% endblock %}
